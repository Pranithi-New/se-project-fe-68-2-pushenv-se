# NEXT_PUBLIC_API_URL is no longer needed.
# Use BACKEND_URL to tell the Next.js server where to proxy API requests.
# This is only needed locally if your backend runs on a different port than 4000.
BACKEND_URL=http://localhost:4000